export type TokenInfo = {
	/**
	 * Mint address of the token
	 */
	address: string;

	/**
	 * Token Symbol
	 */
	symbol: string;

	/**
	 * Token name
	 */
	name: string;

	/**
	 * URI that contains the token's logo
	 */
	logoURI: string;
}
